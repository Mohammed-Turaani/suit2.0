✅ Suit Configurator Final Bundle

1) Backend:
   - open /backend
   - copy .env.example -> .env
   - put OPENAI_API_KEY
   - npm install
   - npm run dev
   - test: http://localhost:5050/health

2) Frontend:
   - open /frontend
   - copy .env.example -> .env
   - npm install
   - npm run dev
   - open: http://localhost:5173

In the UI use:
- AI Suit buttons (local)
- GPT Business/Classic/Wedding (real AI via backend)
- Fabric sliders (Scale & Roughness)
