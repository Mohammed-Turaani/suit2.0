# 3D Suit Configurator (Final)

## Run Frontend
1) copy `.env.example` -> `.env`
2) `npm install`
3) `npm run dev`

## Run AI Backend (separate)
Use the provided backend folder in the bundle.
Start backend on http://localhost:5050 then press GPT buttons in the UI.
